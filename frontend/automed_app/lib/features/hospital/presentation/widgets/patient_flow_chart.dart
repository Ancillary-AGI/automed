import 'package:flutter/material.dart';

// Minimal patient flow chart stub
class PatientFlowChart extends StatelessWidget {
  const PatientFlowChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: SizedBox(
        height: 120,
        child: Center(child: Text('Patient flow chart placeholder')),
      ),
    );
  }
}
