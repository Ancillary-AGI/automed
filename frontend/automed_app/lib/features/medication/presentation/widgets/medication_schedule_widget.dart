import 'package:flutter/material.dart';

// Minimal medication schedule widget stub
class MedicationScheduleWidget extends StatelessWidget {
  const MedicationScheduleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
        child: Padding(
            padding: EdgeInsets.all(12),
            child: Text('Medication schedule placeholder')));
  }
}
